# Backlog

Out of this session on purpose. Do not pick up unless asked.

- Websockets / SSE for the receipts feed
- Historical charts
- Per-operator receipt pages
- Auth on `/v1/receipts`
- Railway deploy of data-api (MCP discovery failed this session; needs `DATABASE_URL` to project `markov-devnet`)
- Apply `0003_public_receipts.postgres.sql` on hosted Supabase when `DATABASE_URL` is set
- House-operator on-chain ticks (stashed WIP on `mvp-prototype-aeb5`)
