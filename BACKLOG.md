# Backlog

Out of this session on purpose. Do not pick up unless asked.

- Websockets / SSE for the receipts feed
- Historical charts
- Per-operator receipt pages
- Auth on `/v1/receipts`
- Point hosted docs `RECEIPTS_API_URL` at `https://data-api-production-5ac5.up.railway.app` (Vercel build-time)
- Optional: apply `public_receipts` on Supabase `markov-devnet` if that project is still used
- House-operator on-chain ticks (stashed WIP on `mvp-prototype-aeb5`)
