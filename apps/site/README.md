# Markov docs (`apps/site`)

Protocol documentation for this repository. Not the marketing site at markovhq.com.

```bash
bun run --filter @markov/site dev
```

http://127.0.0.1:3001 — concepts, guides, BlockReason, SDK, security.

`bun run --filter @markov/site build` writes static files under `public/` for Vercel.
