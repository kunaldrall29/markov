# Float (`apps/web`)

Consumer console for Markov in this repository. Marketplace, create mandate, live receipts, kill switch.

```bash
# from repo root
bun run dev
```

Float: http://127.0.0.1:3000  
API: http://127.0.0.1:8787  

Demo owner `owner_demo`. No browser wallet. Climax: **Run four-beat demo** → `blocked: over_cap`.

Indexer: `apps/indexer`. Vercel config: `vercel.json` in this folder.
